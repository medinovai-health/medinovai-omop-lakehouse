---
description: End a coding session — commits progress and updates logs
---

Verify the implemented feature end-to-end (API curl, Trino query, or pytest as appropriate).
Update `feature_list.json`: set `passes: true` only for the completed feature (no other edits).
Commit with `feat(F###): [description]`.
Append a new session block to `claude-progress.txt`.
